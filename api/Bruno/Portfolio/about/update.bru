meta {
  name: update
  type: http
  seq: 2
}

put {
  url: {{base_url}}/about
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "title": "New Title",
    "content": "New content about me."
  }
}